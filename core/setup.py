from setuptools import setup, find_packages

setup(
    name="soze_core",
    description="Core elements for Söze",
    url="https://github.com/LucasPickering/soze",
    author="Lucas Pickering",
    packages=find_packages(),
    install_requires=[],
)
