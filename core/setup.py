from setuptools import setup, find_packages

setup(
    name='cc_core',
    description="Core elements for case-control",
    url='https://github.com/LucasPickering/case-control',
    author='Lucas Pickering',
    packages=find_packages(),
    install_requires=[],
)
