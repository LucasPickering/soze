import React from 'react';
import ReactDOM from 'react-dom';
import './bootstrap'; // This has to be imported before the App
import App from './components/App';
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));
